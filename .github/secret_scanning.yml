# GitHub-native secret scanning config.
# Full secret scanning + push protection must also be enabled at the
# repository Settings > Code security page.
paths-ignore:
  - '**/testdata/**'
  - '**/*_test.go'
  - 'docs/**'
